# Cx Admin Search App

## How to run locally:
`npm install`

`npm run start`

### other scripts:
`npm run test` - script runs unit tests (jest, testing-library/react)

`npm run lint` - script runs linter check